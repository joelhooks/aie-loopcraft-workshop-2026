{
  "name": "aie-loopcraft-workshop-2026",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "workshop:pull": "docker compose pull workshop",
    "workshop:build": "docker compose build workshop",
    "workshop:shell": "docker compose run --rm workshop zsh",
    "workshop:pi": "docker compose run --rm workshop zsh -lc 'pi'",
    "workshop:herdr": "docker compose run --rm workshop zsh -lc 'herdr integration install pi >/tmp/herdr-pi-integration.log 2>&1 || true; herdr --session aie-loopcraft-workshop-2026'",
    "workshop:daemon": "node scripts/loop-daemon-stub.mjs",
    "web:install": "cd web && bun install",
    "web:check": "cd web && bun run check",
    "web:build": "cd web && bun run build",
    "web:dev": "cd web && bun run dev --host 0.0.0.0",
    "web:publish": "cd web && bun run build && wzrrd publish --file build --slug aie-loopcraft-workshop-2026"
  }
}
